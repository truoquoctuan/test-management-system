version: "3.4"
services:
  app:
    image: registry.gitlab.bzcom.vn/bzc1/bzc-be:latest
    volumes:
      - bzc-be:/upload
    ports:
      - "5781:8080"
    restart: unless-stopped
    healthcheck:
      interval: 30s
      timeout: 10s
      retries: 5
      test: []
volumes:
  bzc-be: